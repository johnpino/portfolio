import type { Meta, StoryObj } from '@storybook/react'

import {{name}}Skeleton from './{{name}}.skeleton'

const meta: Meta<typeof {{name}}Skeleton> = {
	title: '{{type}}/{{name}}',
	component: {{name}}Skeleton,
	tags: ['autodocs'],
	argTypes: {},
}

export default meta
type Story = StoryObj<typeof {{name}}Skeleton>

export const Default: Story = {
	args: {},
}
